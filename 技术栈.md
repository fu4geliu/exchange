# 系统架构

- 微服务架构：基于 Spring Boot 构建后端服务，实现模块化、高内聚低耦合的服务。
- 消息队列：RocketMQ 用于服务间异步通信、解耦、削峰填谷，尤其适用于交易流程中的异步处理（例如，订单处理、资金结算通知）。
- 前端框架：Vue.js 用于构建交互式、响应式的用户界面，提供良好的用户体验。
- 数据库：MySQL（关系型数据库），负责存储所有核心业务数据。
- 缓存：Redis，作为高性能缓存层，主要用于：
  - 缓存频繁读取但更新不频繁的字典数据、配置信息。
  - 缓存用户会话、登录凭证。
  - 存储短期、高并发的临时数据。
  - 布隆过滤器（Bloom Filter）：结合 Redis 实现，用于快速判断某个数据是否存在（例如，判断某个证件号码是否已注册，有效减少对数据库的查询压力），有效降低缓存穿透的风险。

## 1. 客户服务 (CustomerService)

- 核心职责：负责客户的开户、基本资料管理、资金账户管理、证券账户管理等核心客户信息生命周期管理。
- 数据库：拥有自己的数据库（或至少管理 Customer、CapitalAccount、SecurityAccount、IdType、AssetAccountType 等表）。
- 缓存：使用 Redis 缓存客户基本信息、证件类型、账户类别等不常变动的字典数据。

### 对外接口（与前端交互）

| 接口 | HTTP方法 | 路径 | 请求体 (RequestBody) | 响应体 (ResponseBody) | 描述 |
| --- | --- | --- | --- | --- | --- |
| 开户 | POST | /api/customer/open | CustomerOpenRequest（含客户名称、证件信息、资产账户类别等） | CustomerOpenResponse（含客户ID、资金账户ID、证券账户ID等） | 录入客户开户资料，自动生成客户代码、资金账户、证券账户。 |
| 查询客户基本信息 | GET | /api/customer/{id} | 无 | CustomerInfoResponse | 根据客户ID查询客户基本开户资料。 |
| 更新客户资料 | PUT | /api/customer/{id} | CustomerUpdateRequest | CustomerInfoResponse | 更新客户的非敏感资料。 |

### 与数据库交互

- 创建客户：插入 Customer 表，根据规则生成 customer_id。
- 创建资金账户：插入 CapitalAccount 表，根据 AssetAccountType 模板存入初始资金。
- 创建证券账户：插入 SecurityAccount 表，虚构上海/深圳证券账户。
- 查询客户：从 Customer、CapitalAccount、SecurityAccount 表查询信息。
- 字典数据：从 IdType、AssetAccountType 等表读取字典数据，并缓存到 Redis。

### 与 RocketMQ 交互

- 发送消息：
  - Topic: `customer_opened_event`
    - 消息内容：CustomerOpenedEvent（包含 customer_id, capital_account_id, security_account_id 等核心开户信息）。
    - 目的：通知其他服务（如 TradeService）有新客户开户成功，可能需要进行后续处理或数据初始化。

## 2. 交易服务 (TradeService)

- 核心职责：负责处理客户的委托下单、委托撤销、委托成交等核心交易逻辑，包括交易规则校验、资金与股份的冻结/解冻、扣减/增加。
- 数据库：拥有自己的数据库（或至少管理 Order、Trade、Position、SecurityInfo、CommissionRate 等表）。
- 缓存：使用 Redis 缓存实时行情数据（用于价格校验）、佣金费率、股票基础信息。
- 重要说明：交易服务是核心中的核心，对性能和一致性要求最高。

### 对外接口（与前端交互）

| 接口 | HTTP方法 | 路径 | 请求体 (RequestBody) | 响应体 (ResponseBody) | 描述 |
| --- | --- | --- | --- | --- | --- |
| 委托下单 | POST | /api/trade/order | OrderRequest（含客户ID、资金账户ID、股票代码、价格、数量、买卖方向等） | OrderResponse（含委托ID、委托状态等） | 接收客户委托下单请求，进行规则校验，并冻结资金/股份。 |
| 委托撤销 | POST | /api/trade/cancel | CancelOrderRequest（含委托ID、客户ID） | CancelOrderResponse | 接收客户撤单请求，校验后退回资金/股份，修改委托状态。 |
| 模拟成交 | POST | /api/trade/execute | TradeExecuteRequest（含委托ID、成交价格、成交数量） | TradeExecuteResponse | （供操作员或后台系统调用）模拟撮合系统生成成交，更新资金/持仓。 |
| 查询客户持仓 | GET | /api/trade/position/{customerId}/{capitalAccountId} | 无 | PositionListResponse | 查询客户的实时持仓情况。 |
| 查询委托记录 | GET | /api/trade/orders/{customerId} | （可选查询参数：资金账户ID、证券账户ID） | OrderListResponse | 查询客户的委托记录。 |
| 查询成交记录 | GET | /api/trade/trades/{customerId} | （可选查询参数：资金账户ID、证券账户ID） | TradeListResponse | 查询客户的成交记录。 |

### 与数据库交互

- 委托下单：
  - 查询 SecurityInfo 获取股票信息。
  - 查询 CapitalAccount（通过 customer_id 获取）检查可用资金。
  - 查询 Position（通过 customer_id 获取）检查可用股份。
  - 插入 Order 表，设置初始状态。
  - 更新 CapitalAccount（冻结资金）或 Position（冻结股份）。
  - 重要：这一系列操作需要在一个事务中完成，确保数据一致性。
- 委托撤销：
  - 查询 Order 表确认状态。
  - 更新 Order 表状态为“已撤销”。
  - 更新 CapitalAccount（解冻资金）或 Position（解冻股份）。
  - 重要：事务操作。
- 委托成交：
  - 查询 Order 表获取委托详情。
  - 插入 Trade 表。
  - 更新 Order 表状态（部分成交/已成交）、已成交数量。
  - 更新 CapitalAccount（扣减冻结资金，增加可用资金）或 Position（扣减冻结股份，更新持仓数量）。
  - 重要：事务操作。
- 查询持仓、委托、成交：从 Position、Order、Trade 表查询。
- 字典数据/配置：从 SecurityInfo、CommissionRate 等表读取数据，并缓存到 Redis。

### 与 RocketMQ 交互

- 消费消息：
  - Topic: `customer_opened_event`
    - 目的：当 CustomerService 通知新客户开户成功时，TradeService 可能需要初始化一些客户相关的交易配置或数据结构，或者仅作为记录。
- 发送消息：
  - Topic: `order_created_event`
    - 消息内容：OrderCreatedEvent（含 order_id, customer_id, stock_code, price, quantity 等）。
    - 目的：通知其他服务（如 QuerySyncService）有新的委托单生成，用于实时数据同步。
  - Topic: `order_status_updated_event`
    - 消息内容：OrderStatusUpdatedEvent（含 order_id, old_status, new_status, trade_id 等）。
    - 目的：通知其他服务委托单状态变更（如撤销、部分成交、完全成交），特别是成交后需要更新资金、持仓等。
  - Topic: `trade_executed_event`
    - 消息内容：TradeExecutedEvent（含 trade_id, order_id, customer_id, stock_code, trade_price, trade_quantity 等）。
    - 目的：通知其他服务有新的成交发生，用于后续查询聚合与数据同步。

## 3. 查询与数据同步服务 (QuerySyncService)

- 核心职责：
  - 提供客户历史委托与成交记录的聚合查询（如果 TradeService 的查询只返回明细）。
  - 同步并清洗交易事件数据，服务于统计查询能力。
- 数据库：可能不直接管理核心业务数据表，而是从其他服务同步数据。如果仅作查询，它可能直接查询 TradeService 的数据库副本或通过 API 获取。
- 缓存：使用 Redis 缓存聚合后的查询结果，提升统计查询性能。

### 对外接口（与前端交互）

| 接口 | HTTP方法 | 路径 | 请求体 (RequestBody) | 响应体 (ResponseBody) | 描述 |
| --- | --- | --- | --- | --- | --- |
| 查询历史委托统计 | GET | /api/query/orders/summary/{customerId} | （可选查询参数：时间范围、股票代码） | OrderSummaryResponse | 查询客户历史委托的统计信息（如总委托量、已成交量等）。 |
| 查询历史成交统计 | GET | /api/query/trades/summary/{customerId} | （可选查询参数：时间范围、股票代码） | TradeSummaryResponse | 查询客户历史成交的统计信息（如总成交额、总收益等）。 |

### 与数据库交互

- 直接查询 TradeService 所在的 MySQL 数据库，执行更复杂的聚合查询。

### 与 RocketMQ 交互

- 消费消息：
  - Topic: `order_created_event`
  - Topic: `order_status_updated_event`
  - Topic: `trade_executed_event`
  - Topic: `customer_opened_event`
    - 目的：监听所有相关的业务事件，将这些事件的数据进行转换和清洗，并更新自身缓存数据用于提供聚合查询。
