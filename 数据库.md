CREATE TABLE `dict_trading_market` (
                                       `id` TINYINT NOT NULL AUTO_INCREMENT COMMENT '市场ID (使用TINYINT节省空间，预计市场数量不多)',
                                       `market_code` VARCHAR(10) NOT NULL UNIQUE COMMENT '市场代码 (如: SH, SZ, HK, US)',
                                       `market_name` VARCHAR(50) NOT NULL COMMENT '市场名称 (如: 上海证券交易所)',
                                       PRIMARY KEY (`id`),
                                       INDEX `idx_market_code` (`market_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='交易市场字典表';
CREATE TABLE `dict_currency` (
                                 `id` TINYINT NOT NULL AUTO_INCREMENT COMMENT '币种ID (使用TINYINT节省空间)',
                                 `currency_code` VARCHAR(5) NOT NULL UNIQUE COMMENT '币种代码 (如: CNY, USD, HKD)',
                                 `currency_name` VARCHAR(50) NOT NULL COMMENT '币种名称 (如: 人民币, 美元)',
                                 PRIMARY KEY (`id`),
                                 INDEX `idx_currency_code` (`currency_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='币种字典表';
CREATE TABLE `dict_credential_type` (
                                        `id` TINYINT NOT NULL AUTO_INCREMENT COMMENT '证件类型ID (使用TINYINT节省空间)',
                                        `type_code` VARCHAR(10) NOT NULL UNIQUE COMMENT '证件类型代码 (如: ID_CARD, PASSPORT)',
                                        `type_name` VARCHAR(50) NOT NULL COMMENT '证件类型名称 (如: 身份证, 护照)',
                                        PRIMARY KEY (`id`),
                                        INDEX `idx_type_code` (`type_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='证件类型字典表';
CREATE TABLE `dict_order_status` (
                                     `id` TINYINT NOT NULL AUTO_INCREMENT COMMENT '状态ID (使用TINYINT节省空间)',
                                     `status_code` VARCHAR(10) NOT NULL UNIQUE COMMENT '状态代码 (如: PENDING, FILLED, CANCELED)',
                                     `status_name` VARCHAR(50) NOT NULL COMMENT '状态名称 (如: 待提交, 已成交, 已撤销)',
                                     PRIMARY KEY (`id`),
                                     INDEX `idx_status_code` (`status_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='委托状态字典表';
CREATE TABLE `dict_security_category` (
                                          `id` TINYINT NOT NULL AUTO_INCREMENT COMMENT '类别ID (使用TINYINT节省空间)',
                                          `category_code` VARCHAR(10) NOT NULL UNIQUE COMMENT '类别代码 (如: STOCK, BOND, FUND)',
                                          `category_name` VARCHAR(50) NOT NULL COMMENT '类别名称 (如: 股票, 债券, 基金)',
                                          PRIMARY KEY (`id`),
                                          INDEX `idx_category_code` (`category_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='证券类别字典表';
CREATE TABLE `dict_trade_direction` (
                                        `id` TINYINT NOT NULL AUTO_INCREMENT COMMENT '方向ID (使用TINYINT节省空间)',
                                        `direction_code` VARCHAR(10) NOT NULL UNIQUE COMMENT '方向代码 (如: BUY, SELL)',
                                        `direction_name` VARCHAR(50) NOT NULL COMMENT '方向名称 (如: 买入, 卖出)',
                                        PRIMARY KEY (`id`),
                                        INDEX `idx_direction_code` (`direction_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='交易方向字典表';
CREATE TABLE `account_category` (
                                    `id` TINYINT NOT NULL AUTO_INCREMENT COMMENT '账户类别ID',
                                    `category_code` VARCHAR(20) NOT NULL UNIQUE COMMENT '账户类别代码 (如: STANDARD, VIP)',
                                    `category_name` VARCHAR(50) NOT NULL COMMENT '账户类别名称 (如: 标准账户, VIP账户)',
                                    `initial_deposit_amount` DECIMAL(18, 2) DEFAULT 0.00 COMMENT '初始存款金额要求',
                                    `description` VARCHAR(255) COMMENT '类别描述',
                                    `is_active` BOOLEAN DEFAULT TRUE COMMENT '是否活跃',
                                    `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
                                    `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
                                    PRIMARY KEY (`id`),
                                    INDEX `idx_category_code` (`category_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='资产账户类别表';
CREATE TABLE `dict_account_status` (
                                       `id` TINYINT NOT NULL AUTO_INCREMENT COMMENT '状态ID',
                                       `status_code` VARCHAR(20) NOT NULL UNIQUE COMMENT '账户状态代码 (如: ACTIVE, SUSPENDED, CLOSED)',
                                       `status_name` VARCHAR(50) NOT NULL COMMENT '账户状态名称 (如: 活跃, 已暂停, 已关闭)',
                                       PRIMARY KEY (`id`),
                                       INDEX `idx_status_code` (`status_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='资产账户状态字典表';
CREATE TABLE `customer` (
                            `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '客户ID',
                            `customer_code` VARCHAR(20) NOT NULL UNIQUE COMMENT '客户代码 (自动生成，用于高并发场景快速查找)',
                            `username` VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
                            `password` VARCHAR(255) NOT NULL COMMENT '密码 (加密后)',
                            `real_name` VARCHAR(50) NOT NULL COMMENT '真实姓名',
                            `email` VARCHAR(100) UNIQUE COMMENT '邮箱',
                            `phone_number` VARCHAR(20) UNIQUE COMMENT '电话号码',
                            `credential_type_id` TINYINT NOT NULL COMMENT '证件类型ID (外键关联dict_credential_type)',
                            `credential_number` VARCHAR(50) NOT NULL COMMENT '证件号码',
                            `account_category_id` TINYINT NOT NULL COMMENT '资产账户类别ID (外键关联account_category)',
                            `account_status_id` TINYINT NOT NULL COMMENT '账户状态ID (外键关联dict_account_status)',
                            `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
                            `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
                            PRIMARY KEY (`id`),
                            UNIQUE KEY `uk_customer_credential` (`credential_type_id`, `credential_number`),
                            INDEX `idx_customer_code` (`customer_code`),
                            INDEX `idx_username` (`username`),
                            INDEX `idx_phone_number` (`phone_number`),
                            FOREIGN KEY (`credential_type_id`) REFERENCES `dict_credential_type` (`id`),
                            FOREIGN KEY (`account_category_id`) REFERENCES `account_category` (`id`),
                            FOREIGN KEY (`account_status_id`) REFERENCES `dict_account_status` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='客户信息表';
CREATE TABLE `security_info` (
                                 `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '证券ID',
                                 `security_code` VARCHAR(20) NOT NULL UNIQUE COMMENT '证券代码 (如: 600000, 00700)',
                                 `security_name` VARCHAR(100) NOT NULL COMMENT '证券名称 (如: 浦发银行, 腾讯控股)',
                                 `security_category_id` TINYINT NOT NULL COMMENT '证券类别ID (外键关联dict_security_category)',
                                 `market_id` TINYINT NOT NULL COMMENT '所属市场ID (外键关联dict_trading_market)',
                                 `current_price` DECIMAL(18, 4) COMMENT '当前价格 (可根据业务需求，实时行情可从Redis获取)',
                                 `stamp_tax_rate` DECIMAL(8, 6) DEFAULT 0.001 COMMENT '印花税率 (如: 0.001 代表0.1%)',
                                 `min_trade_unit` INT DEFAULT 1 COMMENT '最小交易单位 (如: 1手=100股)',
                                 `listing_date` DATE COMMENT '上市日期',
                                 `is_active` BOOLEAN DEFAULT TRUE COMMENT '是否活跃',
                                 `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
                                 `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
                                 PRIMARY KEY (`id`),
                                 INDEX `idx_security_code` (`security_code`),
                                 INDEX `idx_market_category` (`market_id`, `security_category_id`),
                                 FOREIGN KEY (`security_category_id`) REFERENCES `dict_security_category` (`id`),
                                 FOREIGN KEY (`market_id`) REFERENCES `dict_trading_market` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='证券信息表';
CREATE TABLE `commission_rate` (
                                   `id` INT NOT NULL AUTO_INCREMENT COMMENT '费率ID',
                                   `account_category_id` TINYINT NOT NULL COMMENT '账户类别ID (外键关联account_category)',
                                   `market_id` TINYINT NOT NULL COMMENT '市场ID (外键关联dict_trading_market)',
                                   `commission_rate` DECIMAL(8, 6) NOT NULL COMMENT '佣金费率 (如: 0.0003 代表万分之三)',
                                   `min_commission` DECIMAL(10, 2) DEFAULT 0.00 COMMENT '最低佣金',
                                   `max_commission` DECIMAL(10, 2) COMMENT '最高佣金 (可为空，表示无上限)',
                                   `effective_date` DATE NOT NULL COMMENT '生效日期',
                                   `expiry_date` DATE COMMENT '失效日期 (可为空，表示长期有效)',
                                   `is_active` BOOLEAN DEFAULT TRUE COMMENT '是否活跃',
                                   `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
                                   `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
                                   PRIMARY KEY (`id`),
                                   UNIQUE KEY `uk_account_market_effective` (`account_category_id`, `market_id`, `effective_date`),
                                   INDEX `idx_market_account_category` (`market_id`, `account_category_id`),
                                   FOREIGN KEY (`account_category_id`) REFERENCES `account_category` (`id`),
                                   FOREIGN KEY (`market_id`) REFERENCES `dict_trading_market` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='佣金费率表';
CREATE TABLE `customer_fund_account` (
                                         `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '资金账户ID',
                                         `customer_id` BIGINT NOT NULL UNIQUE COMMENT '客户ID (外键关联customer)',
                                         `account_number` VARCHAR(30) NOT NULL UNIQUE COMMENT '资金账号 (自动生成)',
                                         `currency_id` TINYINT NOT NULL COMMENT '币种ID (外键关联dict_currency)',
                                         `balance` DECIMAL(18, 2) NOT NULL DEFAULT 0.00 COMMENT '可用余额',
                                         `frozen_balance` DECIMAL(18, 2) NOT NULL DEFAULT 0.00 COMMENT '冻结余额',
                                         `total_asset` DECIMAL(18, 2) NOT NULL DEFAULT 0.00 COMMENT '总资产 (实时计算或定期更新)',
                                         `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
                                         `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
                                         PRIMARY KEY (`id`),
                                         INDEX `idx_account_number` (`account_number`),
                                         FOREIGN KEY (`customer_id`) REFERENCES `customer` (`id`),
                                         FOREIGN KEY (`currency_id`) REFERENCES `dict_currency` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='客户资金账户表';
CREATE TABLE `customer_position` (
                                     `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '持仓ID',
                                     `customer_id` BIGINT NOT NULL COMMENT '客户ID (外键关联customer)',
                                     `security_id` BIGINT NOT NULL COMMENT '证券ID (外键关联security_info)',
                                     `market_id` TINYINT NOT NULL COMMENT '所属市场ID (外键关联dict_trading_market)',
                                     `total_quantity` DECIMAL(18, 4) NOT NULL DEFAULT 0.0000 COMMENT '总持仓数量',
                                     `available_quantity` DECIMAL(18, 4) NOT NULL DEFAULT 0.0000 COMMENT '可用持仓数量',
                                     `frozen_quantity` DECIMAL(18, 4) NOT NULL DEFAULT 0.0000 COMMENT '冻结持仓数量',
                                     `cost_price` DECIMAL(18, 4) NOT NULL DEFAULT 0.0000 COMMENT '持仓成本价 (平均成本)',
                                     `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
                                     `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
                                     PRIMARY KEY (`id`),
                                     UNIQUE KEY `uk_customer_security_market` (`customer_id`, `security_id`, `market_id`),
                                     INDEX `idx_customer_id` (`customer_id`),
                                     FOREIGN KEY (`customer_id`) REFERENCES `customer` (`id`),
                                     FOREIGN KEY (`security_id`) REFERENCES `security_info` (`id`),
                                     FOREIGN KEY (`market_id`) REFERENCES `dict_trading_market` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='客户持仓表';
CREATE TABLE `order_log` (
                             `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '委托ID',
                             `customer_id` BIGINT NOT NULL COMMENT '客户ID (外键关联customer)',
                             `account_number` VARCHAR(30) NOT NULL COMMENT '资金账号',
                             `security_id` BIGINT NOT NULL COMMENT '证券ID (外键关联security_info)',
                             `market_id` TINYINT NOT NULL COMMENT '所属市场ID (外键关联dict_trading_market)',
                             `order_code` VARCHAR(32) NOT NULL UNIQUE COMMENT '委托编号 (唯一，高并发下业务生成)',
                             `direction_id` TINYINT NOT NULL COMMENT '交易方向ID (外键关联dict_trade_direction)',
                             `price` DECIMAL(18, 4) NOT NULL COMMENT '委托价格',
                             `quantity` DECIMAL(18, 4) NOT NULL COMMENT '委托数量',
                             `traded_quantity` DECIMAL(18, 4) NOT NULL DEFAULT 0.0000 COMMENT '已成交数量',
                             `status_id` TINYINT NOT NULL COMMENT '委托状态ID (外键关联dict_order_status)',
                             `order_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '委托时间',
                             `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
                             PRIMARY KEY (`id`),
                             INDEX `idx_customer_id` (`customer_id`),
                             INDEX `idx_order_code` (`order_code`),
                             INDEX `idx_security_id_market_status` (`security_id`, `market_id`, `status_id`),
                             FOREIGN KEY (`customer_id`) REFERENCES `customer` (`id`),
                             FOREIGN KEY (`security_id`) REFERENCES `security_info` (`id`),
                             FOREIGN KEY (`market_id`) REFERENCES `dict_trading_market` (`id`),
                             FOREIGN KEY (`direction_id`) REFERENCES `dict_trade_direction` (`id`),
                             FOREIGN KEY (`status_id`) REFERENCES `dict_order_status` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='委托单表';
CREATE TABLE `trade_log` (
                             `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '成交ID',
                             `order_id` BIGINT NOT NULL COMMENT '委托ID (外键关联order_log)',
                             `customer_id` BIGINT NOT NULL COMMENT '客户ID (冗余)',
                             `security_id` BIGINT NOT NULL COMMENT '证券ID (冗余)',
                             `market_id` TINYINT NOT NULL COMMENT '所属市场ID (冗余)',
                             `trade_code` VARCHAR(32) NOT NULL UNIQUE COMMENT '成交编号 (唯一，业务生成)',
                             `direction_id` TINYINT NOT NULL COMMENT '交易方向ID (冗余)',
                             `trade_price` DECIMAL(18, 4) NOT NULL COMMENT '成交价格',
                             `trade_quantity` DECIMAL(18, 4) NOT NULL COMMENT '成交数量',
                             `trade_amount` DECIMAL(18, 2) NOT NULL COMMENT '成交金额',
                             `commission_fee` DECIMAL(10, 2) NOT NULL DEFAULT 0.00 COMMENT '佣金',
                             `stamp_tax` DECIMAL(10, 2) NOT NULL DEFAULT 0.00 COMMENT '印花税',
                             `trade_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '成交时间',
                             PRIMARY KEY (`id`),
                             INDEX `idx_order_id` (`order_id`),
                             INDEX `idx_trade_code` (`trade_code`),
                             INDEX `idx_customer_id_security_id` (`customer_id`, `security_id`),
                             FOREIGN KEY (`order_id`) REFERENCES `order_log` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='成交单表';
CREATE TABLE `cancel_log` (
                              `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '撤单ID',
                              `order_id` BIGINT NOT NULL UNIQUE COMMENT '委托ID (外键关联order_log，一个委托只能成功撤销一次)',
                              `customer_id` BIGINT NOT NULL COMMENT '客户ID (冗余)',
                              `security_id` BIGINT NOT NULL COMMENT '证券ID (冗余)',
                              `market_id` TINYINT NOT NULL COMMENT '所属市场ID (冗余)',
                              `cancel_code` VARCHAR(32) NOT NULL UNIQUE COMMENT '撤单编号 (唯一，业务生成)',
                              `cancel_quantity` DECIMAL(18, 4) NOT NULL COMMENT '撤销数量',
                              `cancel_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '撤单时间',
                              `status` VARCHAR(20) NOT NULL DEFAULT 'SUCCESS' COMMENT '撤单状态 (SUCCESS, FAILED)',
                              PRIMARY KEY (`id`),
                              INDEX `idx_order_id` (`order_id`),
                              INDEX `idx_customer_id` (`customer_id`),
                              FOREIGN KEY (`order_id`) REFERENCES `order_log` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='撤单表';
CREATE TABLE `fund_flow` (
                             `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '流水ID',
                             `customer_id` BIGINT NOT NULL COMMENT '客户ID',
                             `account_number` VARCHAR(30) NOT NULL COMMENT '资金账号',
                             `currency_id` TINYINT NOT NULL COMMENT '币种ID',
                             `flow_type` VARCHAR(20) NOT NULL COMMENT '流水类型 (如: DEPOSIT, WITHDRAW, BUY_FROZEN, BUY_UNFROZEN, SELL_INCOME)',
                             `amount` DECIMAL(18, 2) NOT NULL COMMENT '变动金额 (正数表示增加，负数表示减少)',
                             `before_balance` DECIMAL(18, 2) NOT NULL COMMENT '变动前余额',
                             `after_balance` DECIMAL(18, 2) NOT NULL COMMENT '变动后余额',
                             `relate_order_id` BIGINT COMMENT '关联委托ID',
                             `relate_trade_id` BIGINT COMMENT '关联成交ID',
                             `flow_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '流水时间',
                             PRIMARY KEY (`id`),
                             INDEX `idx_customer_id_account_number` (`customer_id`, `account_number`),
                             INDEX `idx_flow_time` (`flow_time`),
                             INDEX `idx_relate_order_id` (`relate_order_id`),
                             INDEX `idx_relate_trade_id` (`relate_trade_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='资金流水表';
CREATE TABLE `position_flow` (
                                 `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '流水ID',
                                 `customer_id` BIGINT NOT NULL COMMENT '客户ID',
                                 `security_id` BIGINT NOT NULL COMMENT '证券ID',
                                 `market_id` TINYINT NOT NULL COMMENT '市场ID',
                                 `flow_type` VARCHAR(20) NOT NULL COMMENT '流水类型 (如: BUY, SELL, FROZEN, UNFROZEN)',
                                 `quantity` DECIMAL(18, 4) NOT NULL COMMENT '变动数量 (正数表示增加，负数表示减少)',
                                 `before_total_quantity` DECIMAL(18, 4) NOT NULL COMMENT '变动前总持仓',
                                 `after_total_quantity` DECIMAL(18, 4) NOT NULL COMMENT '变动后总持仓',
                                 `relate_order_id` BIGINT COMMENT '关联委托ID',
                                 `relate_trade_id` BIGINT COMMENT '关联成交ID',
                                 `flow_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '流水时间',
                                 PRIMARY KEY (`id`),
                                 INDEX `idx_customer_id_security_id` (`customer_id`, `security_id`),
                                 INDEX `idx_flow_time` (`flow_time`),
                                 INDEX `idx_relate_order_id` (`relate_order_id`),
                                 INDEX `idx_relate_trade_id` (`relate_trade_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='持仓流水表';
-- dict_trading_market (交易市场字典表) - id为AUTO_INCREMENT，不指定id
INSERT INTO `dict_trading_market` (`market_code`, `market_name`) VALUES
                                                                     ('SZ', '深交所'),
                                                                     ('SH', '上交所');

-- dict_currency (币种字典表) - id为AUTO_INCREMENT，不指定id
INSERT INTO `dict_currency` (`currency_code`, `currency_name`) VALUES
                                                                   ('CNY', '人民币'),
                                                                   ('HKD', '港币'),
                                                                   ('USD', '美元');

-- dict_credential_type (证件类型字典表) - id为AUTO_INCREMENT，不指定id
INSERT INTO `dict_credential_type` (`type_code`, `type_name`) VALUES
                                                                  ('ID_CARD', '身份证'),
                                                                  ('PASSPORT', '护照'),
                                                                  ('MIL_CARD', '军官证'); -- 假设 '02' 对应 '军官证'

-- dict_trade_direction (交易方向字典表) - id为AUTO_INCREMENT，不指定id
INSERT INTO `dict_trade_direction` (`direction_code`, `direction_name`) VALUES
                                                                            ('BUY', '买入'), -- 假设 'B' 对应 '买入'
                                                                            ('SELL', '卖出'); -- 假设 'S' 对应 '卖出'

-- dict_order_status (委托状态字典表) - id为AUTO_INCREMENT，不指定id
-- dict_order_status (委托状态字典表) - 尝试使用更短的status_code-- dict_order_status (委托状态字典表)
INSERT INTO `dict_order_status` (`status_code`, `status_name`) VALUES
                                                                   ('UNREPORTED', '未报'),
                                                                   ('REPORTING', '正报'),
                                                                   ('REPORTED', '已报'),
                                                                   ('REP_CANC', '已报待撤'),       -- 进一步缩短 'REP_CANCEL'
                                                                   ('PF_CANC', '部成待撤'),        -- 进一步缩短 'PART_FILL_CANCEL'
                                                                   ('PART_CANC', '部撤'),          -- 进一步缩短 'PART_CANCELED'
                                                                   ('CANCELED', '已撤'),
                                                                   ('PART_FILL', '部成'),          -- 缩短 'PARTIAL_FILLED'
                                                                   ('FILLED', '已成'),
                                                                   ('VOID', '废单'),
                                                                   ('TO_REPORT', '待报');

-- dict_security_category (证券类别字典表) - id为AUTO_INCREMENT，不指定id
INSERT INTO `dict_security_category` (`category_code`, `category_name`) VALUES
    ('STOCK', '股票'); -- 假设 'S0' 对应 '股票'

-- dict_account_status (资产账户状态字典表) - id为AUTO_INCREMENT，不指定id
INSERT INTO `dict_account_status` (`status_code`, `status_name`) VALUES
                                                                     ('NORMAL', '正常'),
                                                                     ('FROZEN', '冻结'),
                                                                     ('ABNORMAL', '异常'), -- 假设 '8' 对应 '异常'
                                                                     ('CANCELED', '注销'); -- 假设 '9' 对应 '注销'

-- account_category (资产账户类别表) - id为AUTO_INCREMENT，不指定id
INSERT INTO `account_category` (`category_code`, `category_name`, `initial_deposit_amount`, `description`, `is_active`) VALUES
                                                                                                                            ('RETAIL', '散户', 50000.00, '散户账户', TRUE),
                                                                                                                            ('INTERMEDIATE', '中户', 100000.00, '中户账户', TRUE),
                                                                                                                            ('MAJOR', '大户', 500000.00, '大户账户', TRUE),
                                                                                                                            ('INSTITUTIONAL', '机构', 1000000.00, '机构账户', TRUE);

-- security_info (证券信息表) - id为AUTO_INCREMENT，不指定id
INSERT INTO `security_info` (`security_code`, `security_name`, `security_category_id`, `market_id`, `current_price`, `stamp_tax_rate`, `min_trade_unit`, `listing_date`, `is_active`) VALUES
                                                                                                                                                                                          ('000001', '平安银行', (SELECT id FROM dict_security_category WHERE category_code = 'STOCK'), (SELECT id FROM dict_trading_market WHERE market_code = 'SZ'), 9.81, 0.001, 100, '2007-04-10', TRUE),
                                                                                                                                                                                          ('000651', '格力电器', (SELECT id FROM dict_security_category WHERE category_code = 'STOCK'), (SELECT id FROM dict_trading_market WHERE market_code = 'SZ'), 36.98, 0.001, 100, '1996-11-18', TRUE),
                                                                                                                                                                                          ('000858', '五粮液', (SELECT id FROM dict_security_category WHERE category_code = 'STOCK'), (SELECT id FROM dict_trading_market WHERE market_code = 'SZ'), 136.15, 0.001, 100, '1998-04-21', TRUE),
                                                                                                                                                                                          ('600446', '金证股份', (SELECT id FROM dict_security_category WHERE category_code = 'STOCK'), (SELECT id FROM dict_trading_market WHERE market_code = 'SH'), 12.85, 0.001, 100, '2003-12-25', TRUE),
                                                                                                                                                                                          ('600900', '长江电力', (SELECT id FROM dict_security_category WHERE category_code = 'STOCK'), (SELECT id FROM dict_trading_market WHERE market_code = 'SH'), 25.20, 0.001, 100, '2003-11-18', TRUE),
                                                                                                                                                                                          ('601857', '中国石油', (SELECT id FROM dict_security_category WHERE category_code = 'STOCK'), (SELECT id FROM dict_trading_market WHERE market_code = 'SH'), 8.65, 0.001, 100, '2007-11-05', TRUE);

-- commission_rate (佣金费率表) - id为AUTO_INCREMENT，不指定id
INSERT INTO `commission_rate` (`account_category_id`, `market_id`, `commission_rate`, `min_commission`, `effective_date`, `is_active`) VALUES
                                                                                                                                           ((SELECT id FROM account_category WHERE category_code = 'RETAIL'), (SELECT id FROM dict_trading_market WHERE market_code = 'SZ'), 0.001, 5.00, '2023-01-01', TRUE),
                                                                                                                                           ((SELECT id FROM account_category WHERE category_code = 'RETAIL'), (SELECT id FROM dict_trading_market WHERE market_code = 'SH'), 0.001, 5.00, '2023-01-01', TRUE),
                                                                                                                                           ((SELECT id FROM account_category WHERE category_code = 'INTERMEDIATE'), (SELECT id FROM dict_trading_market WHERE market_code = 'SZ'), 0.0008, 5.00, '2023-01-01', TRUE),
                                                                                                                                           ((SELECT id FROM account_category WHERE category_code = 'INTERMEDIATE'), (SELECT id FROM dict_trading_market WHERE market_code = 'SH'), 0.0008, 5.00, '2023-01-01', TRUE),
                                                                                                                                           ((SELECT id FROM account_category WHERE category_code = 'MAJOR'), (SELECT id FROM dict_trading_market WHERE market_code = 'SZ'), 0.0006, 5.00, '2023-01-01', TRUE),
                                                                                                                                           ((SELECT id FROM account_category WHERE category_code = 'MAJOR'), (SELECT id FROM dict_trading_market WHERE market_code = 'SH'), 0.0006, 5.00, '2023-01-01', TRUE),
                                                                                                                                           ((SELECT id FROM account_category WHERE category_code = 'INSTITUTIONAL'), (SELECT id FROM dict_trading_market WHERE market_code = 'SZ'), 0.0003, 5.00, '2023-01-01', TRUE),
                                                                                                                                           ((SELECT id FROM account_category WHERE category_code = 'INSTITUTIONAL'), (SELECT id FROM dict_trading_market WHERE market_code = 'SH'), 0.0003, 5.00, '2023-01-01', TRUE);