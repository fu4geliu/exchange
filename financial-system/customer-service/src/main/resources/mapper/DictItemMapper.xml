<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qianhua.customer.mapper.DictItemMapper">

    <select id="listByDictCode" resultType="com.qianhua.customer.entity.DictItem">
        SELECT item_code AS itemCode, item_name AS itemName, sort_no AS sortNo
        FROM dict_item
        WHERE dict_code = #{dictCode}
        ORDER BY sort_no ASC, item_code ASC
    </select>

    <select id="countItem" resultType="int">
        SELECT COUNT(1) FROM dict_item WHERE dict_code = #{dictCode} AND item_code = #{itemCode}
    </select>
</mapper>
